#!/usr/bin/env bash
# ghpr-release — Quick alias for release commits
# Usage: ghpr-release "version"
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
"$SCRIPT_DIR/bin/ghpr" -m "release: v${*:-}" "${@:2}"
